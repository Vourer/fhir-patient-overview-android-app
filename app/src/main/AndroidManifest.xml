<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.vourer.fhirpatientoverview">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FHIRPatientOverview"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".activities.GraphActivity"
            android:windowSoftInputMode="adjustPan" />
        <activity android:name=".activities.ResourceDetailsActivity" />
        <activity
            android:name=".activities.PatientResourcesActivity"
            android:windowSoftInputMode="adjustPan" />
        <activity android:name=".activities.EditPatientActivity" />
        <activity
            android:name=".activities.PatientDetailsActivity"
            android:label="@string/title_activity_patient_details" />
        <activity
            android:name=".activities.MainActivity"
            android:windowSoftInputMode="adjustPan">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>